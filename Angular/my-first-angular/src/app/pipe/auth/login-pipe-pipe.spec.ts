import { LoginPipePipe } from './login-pipe-pipe';

describe('LoginPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LoginPipePipe();
    expect(pipe).toBeTruthy();
  });
});
